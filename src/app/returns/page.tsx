import StaticPage from "@/components/StaticPage";

export default function ReturnsPage() {
  return (
    <StaticPage
      title="Returns"
      description="We are preparing a simple, concierge-level returns process. Details will appear here soon."
    />
  );
}
